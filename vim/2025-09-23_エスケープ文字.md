### Vim正規表現の特殊文字

   `*`: 直前の文字の0回以上の繰り返しを表す 

   `.`: 任意の一文字

   `^`: 行頭

   `$`: 行末

   `[` `]`: 文字の集合

   ` \  `: バックスラッシュ自体を検索する場合

   ` \n  `: 検索するときの改行

   ` \r  `: 置換するときの改行

これらの文字を文字通りに扱いたい場合は、`\`（バックスラッシュ、円マーク）を前につけてエスケープしてください。
- 連続する改行を1行にまとめる。
  :%s/\n\n\+/\r/g
